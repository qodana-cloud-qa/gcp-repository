version: 1.0

# ==== FROM LOCAL ====
bootstrap: ./setup.sh

# ==== FROM GLOBAL ====
failureConditions:
    severityThresholds:
        critical: 0

# ==== LOCAL THAT OVERRIDEN GLOBAL (global was global-profile.yaml) ====
profile:
    path: local-profile.yaml
